import { Module } from '@nestjs/common';

/**
 * GraphModule — code graph exploration: node details, neighborhood, and the
 * repository graph (GET /api/graph, GET /api/nodes/:id, Phase 4+).
 *
 * Phase 2 establishes the module boundary only; no domain logic is
 * implemented yet.
 */
@Module({})
export class GraphModule {}
