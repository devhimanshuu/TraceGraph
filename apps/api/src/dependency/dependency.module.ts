import { Module } from '@nestjs/common';

/**
 * DependencyModule — dependencies, dependents, callers, and tests of an
 * entity (GET /api/nodes/:id/dependencies|dependents|callers|tests, Phase 5+).
 *
 * Phase 2 establishes the module boundary only; no domain logic is
 * implemented yet.
 */
@Module({})
export class DependencyModule {}
